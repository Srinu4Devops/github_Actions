name: script excution by srinivas
on:
  workflow_dispatch:
   paths: [Templates_Two/**]
   inputs:
     environment:
        description: 'Environment to run tests against'
        type: environment
        required: true
 
jobs:
  build:
   runs-on: ubuntu-latest
   environment: Development
   steps:
    - name: echo a string
      run: echo "Hello Srini! - Developement"
    - name: multiline script
      run: |
        node -v
        npm -v

  scripting:
   runs-on: ubuntu-latest
   environment: Production
   steps:
    - name: echo a string
      run: echo "Hello Srini! - production"
    - name: multiline script
      run: |
       node -v
       npm -v

